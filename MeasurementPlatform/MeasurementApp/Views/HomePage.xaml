<Page
    x:Class="MeasurementApp.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:MeasurementApp.Controls"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" Margin="{StaticResource MediumLeftRightMargin}">
        <Grid
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <!--
                The SystemControlPageBackgroundChromeLowBrush background represents where you should place your content.
                Place your content here.
            -->
            <!--<TextBlock Text="{x:Bind ViewModel.TestText}"/>
            <Button Content="Press Me" Command="{x:Bind ViewModel.DialogCommand}"/>-->

            <controls:ConnectionControl x:Name="MachineConnectionControl" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0 10 0 0"/>

        </Grid>
    </Grid>
</Page>
