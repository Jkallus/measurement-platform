<Page
    x:Class="MeasurementApp.Views.JobSetupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MeasurementApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:MeasurementApp.Controls.JobSetup"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="ContentArea" Margin="{StaticResource MediumLeftRightMargin}">
        <Grid Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="600"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <controls:ScanDisplayControl Width="600" Height="500" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Top"/>
            <controls:ScanSettingsControl Width="Auto" Height="Auto" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20 0 0 0"/>
        </Grid>
    </Grid>
</Page>
